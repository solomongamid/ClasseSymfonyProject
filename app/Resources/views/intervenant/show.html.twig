{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <h1>Intervenant</h1>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ intervenant.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ intervenant.name }}</td>
            </tr>
            <tr>
                <th>Material</th>
                <td>{{ intervenant.material }}</td>
            </tr>
        </tbody>
    </table>

    <div class="form-group">
    
            <a href="{{ path('intervenant_index') }}" class="btn btn-info">Back to the list</a>
    </div>
    <div class="form-group">
            <a href="{{ path('intervenant_edit', { 'id': intervenant.id }) }}" class="btn btn-primary">Edit</a>
    </div>
    <div class="form-group">
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}
    </div>
</div>
{% endblock %}
